fun fib(n: i32) {
    if n < 2 {
        return 1;
    }

    return fib(n - 1) + fib(n - 2);
}

fun main() {
    var sum: i32 = 0;
    for var i: i32 = 0, i < 10, ++i {
        sum += i;
    }
    return sum;
}
